<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bvinstruments.paristravelguide">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".Settings_activity"
            android:configChanges="locale"></activity>
        <activity android:name=".WebActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".TabActivity" />
        <activity android:name=".ItemActivity" />
        <activity android:name=".activities.Restaurants" />
        <activity android:name=".activities.Hotels" />
        <activity android:name=".activities.FitnessClubs" />
        <activity android:name=".activities.CulturalHistoricalSights" />
        <activity android:name=".activities.DetailActivity" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBz_wyswKbQSqzCgBhxIxAVLEqXuSoyEhY" />
    </application>

</manifest>